{
    "name": "battle-app",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/battle-app",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nrwl/next:build",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "root": "packages/battle-app",
                "outputPath": "dist/packages/battle-app"
            },
            "configurations": {
                "development": {
                    "outputPath": "packages/battle-app"
                },
                "production": {}
            }
        },
        "serve": {
            "executor": "@nrwl/next:server",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "battle-app:build",
                "dev": true
            },
            "configurations": {
                "development": {
                    "buildTarget": "battle-app:build:development",
                    "dev": true
                },
                "production": {
                    "buildTarget": "battle-app:build:production",
                    "dev": false
                }
            }
        },
        "export": {
            "executor": "@nrwl/next:export",
            "options": {
                "buildTarget": "battle-app:build:production"
            }
        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["coverage/packages/battle-app"],
            "options": {
                "jestConfig": "packages/battle-app/jest.config.ts",
                "passWithNoTests": true
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["packages/battle-app/**/*.{ts,tsx,js,jsx}"]
            }
        }
    },
    "tags": []
}
